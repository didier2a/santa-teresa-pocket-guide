const APP_VERSION='6.0.6';
const CACHE='santa-teresa-v6-ar-6';
const MAP_CACHE='santa-teresa-map-v6';
const CORE=[
  './','./index.html','./diagnostic-s22.html','./styles.css','./v3.css','./v4b.css','./v5.css','./ar-v6.css','./manifest.webmanifest',
  './data/trip.json','./js/app.js','./js/ar-v6.js','./js/s22-fixes.js','./js/diagnostic-s22.js','./js/schedule-engine.js','./js/trip-config.js','./assets/icons/icon.svg','./assets/icons/icon-192.png','./assets/icons/icon-512.png',
  './assets/photos/guide-map.svg','./assets/photos/modesto.svg'
];
const LEAFLET=['https://unpkg.com/leaflet@1.9.4/dist/leaflet.css','https://unpkg.com/leaflet@1.9.4/dist/leaflet.js'];
async function cacheExternal(cache,url,mode='cors'){try{const request=new Request(url,{mode,credentials:'omit'});const response=await fetch(request);if(response&&(response.ok||response.type==='opaque'))await cache.put(request,response.clone())}catch{}}
async function warmExternal(){const cache=await caches.open(CACHE);await Promise.allSettled(LEAFLET.map(url=>cacheExternal(cache,url,'cors')))}
async function trimCache(name,maxEntries){const cache=await caches.open(name),keys=await cache.keys();while(keys.length>maxEntries)await cache.delete(keys.shift())}
async function refreshOpenClients(){const clients=await self.clients.matchAll({type:'window',includeUncontrolled:true});await Promise.allSettled(clients.map(async client=>{try{const url=new URL(client.url);if(url.origin!==self.location.origin)return;url.searchParams.set('app',APP_VERSION);await client.navigate(url.toString())}catch{}}))}
self.addEventListener('install',event=>{event.waitUntil((async()=>{const cache=await caches.open(CACHE);await cache.addAll(CORE);await warmExternal();await self.skipWaiting()})())});
self.addEventListener('activate',event=>{event.waitUntil((async()=>{const keys=await caches.keys();await Promise.all(keys.filter(k=>![CACHE,MAP_CACHE].includes(k)).map(k=>caches.delete(k)));await self.clients.claim();await refreshOpenClients()})())});
self.addEventListener('fetch',event=>{if(event.request.method!=='GET')return;const url=new URL(event.request.url);if(url.origin===location.origin&&event.request.mode==='navigate'){event.respondWith((async()=>{const cache=await caches.open(CACHE);try{const request=new Request(event.request,{cache:'no-store'});const response=await fetch(request);if(response.ok){if(url.pathname.endsWith('/diagnostic-s22.html'))await cache.put('./diagnostic-s22.html',response.clone());else await cache.put('./index.html',response.clone())}return response}catch{return(await cache.match(url.pathname.endsWith('/diagnostic-s22.html')?'./diagnostic-s22.html':'./index.html'))||new Response('Pocket Guide hors ligne',{status:503,statusText:'Offline'})}})());return}if(url.origin===location.origin){event.respondWith((async()=>{const cache=await caches.open(CACHE),hit=await cache.match(event.request);if(hit)return hit;try{const response=await fetch(event.request,{cache:'no-cache'});if(response.ok)await cache.put(event.request,response.clone());return response}catch{return new Response('',{status:504,statusText:'Offline'})}})());return}if(url.hostname==='unpkg.com'||url.hostname.includes('wikimedia.org')||url.hostname.includes('wikimediausercontent.com')){event.respondWith((async()=>{const cache=await caches.open(CACHE),hit=await cache.match(event.request);if(hit)return hit;try{const response=await fetch(event.request);if(response)await cache.put(event.request,response.clone());return response}catch{return new Response('',{status:504,statusText:'Offline'})}})());return}if(url.hostname.endsWith('tile.openstreetmap.org')){event.respondWith((async()=>{const cache=await caches.open(MAP_CACHE),hit=await cache.match(event.request);if(hit)return hit;try{const response=await fetch(event.request);if(response.ok){await cache.put(event.request,response.clone());void trimCache(MAP_CACHE,180)}return response}catch{return new Response('',{status:504,statusText:'Offline'})}})())}});
